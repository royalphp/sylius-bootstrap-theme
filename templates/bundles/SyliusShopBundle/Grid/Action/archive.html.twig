{% import '@SyliusShop/Common/Macro/buttons.html.twig' as buttons %}

{% set path = options.link.url|default(path(options.link.route|default(grid.requestConfiguration.getRouteName('archive')), options.link.parameters|default({'id': data.id}))) %}
{% set label = data.archivedAt is not null ? action.options.restore_label|default('sylius.ui.restore') : action.label|default('sylius.ui.archive') %}

{{ buttons.archive(path, label) }}
